<div class="show">
      <div class="experiences">
      <br><br>

      <h4 class="all_experiences"><strong> The Collective Experience </strong></h4>

        <br>
        <p>All Experiences:</p>
          <% Experience.all.each do |experience| %>
            <p><%= link_to experience.story, experience_path(experience)%> </p>
          <% end %>

    </div>
</div>
